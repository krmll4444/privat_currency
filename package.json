{
  "name": "privat-rate",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch": "node engine/index.mjs",
    "chat-id": "node engine/get-chat-id.mjs",
    "test": "node --test engine/*.test.mjs",
    "preview": "mkdir -p miniapp/data && cp data/latest.json data/history.jsonl miniapp/data/ && python3 -m http.server 8080 --directory miniapp"
  }
}
